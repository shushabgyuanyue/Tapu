domain: frontend, backend, storage, ecommerce
pack_status: packed
branch: evo/core-domain-closeout
base: tapu-master
description: 前端重构 + 交互防抖与全局动效 + 双轨账号认证 + 实体选购架构 + 转码兼容性修复

## TODO (closure)

- [x] R2 存储服务（server/services/r2.js）
- [x] 9:16 转码 + 封面帧提取（transcode.js）
- [x] Stats API 筛选增强（daily + group + date）
- [x] 官网首页 LandingPage（横向滑动大图窗 + 共情文案重构）
- [x] 管理端样式重写（顶部导航 + 拖拽上传 + 分组弹窗）
- [x] SVG 图表 + 日期筛选组件
- [x] 播放页性能优化（critical CSS + poster + code-split）
- [x] 播放页滑动动效重构（Slide动效 + 250ms防误触/双击分离）
- [x] 社区页（卡片网格 + 全站高定 SVG 图标替换）
- [x] 社交互动后端（interactions + defaults 表 + 私有视频拦截）
- [x] 双轨账户与加密层（users/entities 表 + auth.js 对称加密登录）
- [x] 实体心愿单选购流（支持绑定默认自选）
- [x] 热门统计机制与心愿单排行（popular / default-ranking）
- [x] Vercel / Railway 部署配置
- [x] 转码兼容性与极致压缩（-pix_fmt yuv420p + -crf 28 + fast）
- [ ] R2 实际对接（需 Cloudflare 账号配置环境变量）
- [ ] 二创视频生成（仅前端界面，后端 AI 能力待接入）
- [ ] 移动端真机 NFC 全链路测试

## Post-Mortem

- interactions.js 误用 TypeScript 语法导致后端启动报错 → 后端 .js 文件不经过 tsc 编译，须保持纯 JS
- body overflow:hidden 全局锁死滚动 → 播放页应自行 position:fixed 隔离
- Vite 8 manualChunks 必须为函数形式而非对象
- fluent-ffmpeg .outputOptions('-vf ...') 与 .videoFilter() 冲突 → 用 API 方法
- Vue transition 中的 v-if / v-else 规则导致 Vite 编译阻断 → v-else 必须紧邻 v-if（或直接改用独立判断的 v-if="!expr"）
- Windows PowerShell 环境下执行 npm run dev (&) 导致后台启动进程失效/残留挂起 → 需依赖 node 专门异步脚本或确保存活
- 数据库连接模块无 default 导出时（`export async function getDb`）使用 `import db from` 会触发后端启动崩溃 → 严格保持析构式 `import { getDb }` 并正确 await 异步初始化

packed_by: gemini-cli
packed_at: 2026-07-04
entrypoint: tapu/src/main.ts

## 2026-07-14 Codex Increment

- domain: frontend, product-flow, docs
- ref: 810eb56
- todo:
  - [x] 恢复 `/assets` 明显的 token 绑定入口，顶部 CTA 与空展馆态都能滚动聚焦到绑定输入框。
  - [x] 将商城筛选条和商品卡拆成 `ShopFilterBar`、`ShopProductCard`，`ShopPage.vue` 从 662 行降到 415 行。
  - [x] 压缩商城 hero 与商品卡尺寸，保持黑/紫/粉情绪 IP 风格并改善手机端布局。
  - [x] 更新 `DOMAINS.md` 与 `docs/business-flow.md`，记录商城组件入口和后续拆分优先级。
- evidence:
  - `npm run build`
  - `git diff --check`
  - `node --check tapu/server/routes/auth.js`
  - `node --check tapu/server/routes/dailyStickers.js`
  - `node --check tapu/server/routes/orders.js`
  - `node --check tapu/server/routes/videos.js`
  - 临时 DB 启动服务 smoke：`/api/groups`、`/api/series`、`/api/config/wishlist_enabled`、`/api/daily-stickers/resolve`。
- post-mortem:
  - `AssetsPage.vue` 与 `DailyStickerManage.vue` 仍超过 500 行；本轮只修复资产绑定入口并将后续高收益拆分写入文档，避免一次性大拆影响业务闭环。

## 2026-07-14 Codex Increment 2

- domain: frontend, daily-sticker, auth-asset, docs
- ref: 25aa475
- todo:
  - [x] `DailyStickerPage.vue` 支持分钟级发布边界自动静默刷新，避免 NFC 页面必须手动刷新才更换内容/动画。
  - [x] 账户下拉菜单顺序调整为“我的资产 -> 解绑申诉 -> 账户设置”。
  - [x] 收敛资产展馆和 IP 详情页的大图、高度、阴影与移动端布局。
  - [x] 更新 `DOMAINS.md` 与 `docs/business-flow.md`，记录贴纸自动刷新和导航顺序。
- evidence:
  - `npm run build`
  - `git diff --check`
- post-mortem:
  - 本轮未做移动端真机 NFC 长时间停留测试；自动刷新逻辑仅在无 `day/date` 预览参数时启用，避免后台预览指定天数被自动跳转。
## 2026-07-14 Codex Increment 3

- domain: product-flow, auth-asset, frontend, daily-sticker, docs
- ref: 60c1295
- todo:
  - [x] 用临时 DB、临时上传目录、真实 HTTP 服务跑通情绪 IP 订单/token/绑定/上传/默认内容/NFC resolve/转赠完整流程，测试结束后删除临时数据和脚本。
  - [x] 用临时 DB 跑通日常贴纸人格、世界、故事、条目、token、绑定、按天预览与分钟级 cron 配置流程。
  - [x] 修复持有人设置实体默认内容时 `entity_ownership_events` 缺少 token/order 信息的问题，增强后续实体传承审计价值。
  - [x] 继续优化商城、资产展馆、IP 详情页视觉，使其更接近高端商城与个人收藏展馆，并保持首页黑/紫/粉情绪 IP 方向。
  - [x] 更新 `DOMAINS.md` 与 `docs/business-flow.md`，明确私有内容 NFC 未登录不泄露、登录持有人后可播放的产品口径。
- evidence:
  - 临时 DB 真实服务 HTTP flow：entity order/token/bind/private default/transfer PASS；daily sticker persona/world/story/token/bind/day preview PASS。
  - `npm run build`
  - `git diff --check`
- post-mortem:
  - 私有内容与“碰一下直接播放”存在产品口径张力：当前规则是公开内容可匿名播放，私有内容必须登录持有人账号。后续若希望实体 token 本身授权播放私有内容，需要重新评估隐私风险和分享边界。
  - `AssetsPage.vue`、`DailyStickerManage.vue`、`IPDetailPage.vue` 仍超过 500 行；本轮只做低风险高收益修复，下一轮可优先拆组件。

## 2026-07-16 Codex Increment 4

- domain: frontend, backend, mint-studio, permissions, copy-i18n
- ref: 0c7a205
- branch_context: tapu-master direct closeout; no separate evo branch was active when this loop was handed off.
- todo:
  - [x] 将非官方管理端前端用户可见文案按产品域抽到 `tapu/src/copy/*`，覆盖首页、商城、社区、内容消费、轻应用、创作中心、用户中心和法律页。
  - [x] 将后端用户可见权限/接口错误与 Mint Studio 引导模板抽到 `tapu/server/copy/*`，降低后续国际化和话术调优成本。
  - [x] 保留资源文件名、业务识别关键词、后台管理校验和 seed 内容，不把非展示内容误抽成 UI 文案。
  - [x] 承接前序未提交冲刺内容，确保权限 OS、Mint Studio、轻应用和内容资产链路随本轮一起闭环。
- evidence:
  - `npm run build`
  - `node --check` for changed server copy, middleware, services and route files.
  - `rg -n "[\\p{Han}]" tapu/src/views tapu/src/components -g '!**/admin/**' -g '!**/official/**' -g '!**/copy/**'` only leaves resource names, business matching keywords, skip words and admin pagination.
- post-mortem:
  - 本轮接手时已有多轮未提交改动，且当前分支已经在 `tapu-master`；本次选择完整收口提交，避免只提交 copy 抽取造成前后端状态割裂。
  - 后续应尽量恢复 `evo/<task>` 分支 + `.ledger` TODO 先行，减少主线直接积累大批未提交改动的风险。

## 2026-07-16 Codex Increment 5

- domain: engineering-governance, frontend-api, docs, encoding
- ref: working-tree
- branch_context: tapu-master working tree; this loop intentionally keeps unrelated user note changes out of the sprint scope.
- todo:
  - [x] 增加仓库级 `.editorconfig` 与 `.gitattributes`，固化 UTF-8、LF、二进制文件和 Windows 脚本行尾策略。
  - [x] 增加 `check:encoding`、`check:server`、`check:size`、`check:quality`，把编码、后端 JS 语法和大文件报告纳入本地质量入口。
  - [x] 增加 `use-utf8-terminal.ps1` 与 `check:terminal`，解决 Windows PowerShell 将无 BOM UTF-8 中文文档误读为乱码的问题。
  - [x] 将前端 API 基础请求能力拆到 `tapu/src/api/http.ts`，将 Mint Studio 与轻应用接口拆到独立模块，并保持 `tapu/src/api/index.ts` 兼容 re-export。
  - [x] 拆分 `MintStudioPage.vue`，将已铸造内容库抽成 `useMintStudioLibrary`，将侧边栏抽成 `MintStudioSidebar`，将页面样式抽到 `src/styles/mintStudio.css`。
  - [x] 拆分 `server/routes/dailyStickers.js`，将贴纸发布时间、条目素材、故事进度和资产组装 runtime helpers 抽到 `server/services/dailyStickerRuntime.js`。
  - [x] 将 `routePermissions.js` 收敛为 `PERMISSION_CHECKERS` 函数表，明确一个 permission type 对应一个调用前校验函数。
  - [x] 增加 `server/contracts/routePermissionMap.js`，集中查询当前接口到权限类型的映射，并纳入 legacy direct routes。
  - [x] 增加 `criticalPermissionFlows.js` 与 `check:permissions`，验证关键链路所需接口和权限类型持续存在。
  - [x] 增加 `docs/engineering-governance.md`，沉淀文件体积阈值、编码策略、质量命令和新增功能检查卡。
  - [x] 去除项目域历史文件 UTF-8 BOM，并让编码检查从仓库根扫描，同时排除 `archo evo/` 协议子模块。
- evidence:
  - `npm run check:quality`
  - `npm run check:terminal`
  - dot-source UTF-8 session 后读取 `docs/engineering-governance.md` 中文正常显示
  - `npm run build`
  - `npm run check:permissions`
  - route map spot check: `/api/auth/entity-default-by-token -> token_unbound_or_owner`, `/api/daily-stickers/bind-token -> account_object_claimable`
  - `git diff --check`
- post-mortem:
  - `src/api/index.ts`、`MintStudioPage.vue`、`server/routes/dailyStickers.js` 已从硬债区降到软债区；剩余硬债集中在 `AssetsPage.vue` 与 `src/views/official/DailyStickerManage.vue`。
  - `git diff --check` 在 Windows 下仍会报告行尾规范化 warning，但没有 whitespace error；后续正式提交后 `.gitattributes` 会逐步稳定这些差异。
## 2026-07-16 Codex Increment 6

- domain: engineering-governance, contracts, permissions, app-manifest, database
- ref: working-tree
- branch_context: tapu-master working tree; unrelated local note changes remain outside this sprint scope.
- todo:
  - [x] Added `operation` as a semantic contract layer while keeping permission execution as `interface -> permission type -> checker -> handler`.
  - [x] Extended route contract metadata with operation/body/query/response/errors/tags and covered the most expensive permission flows.
  - [x] Added `test:contracts` and critical route contract assertions.
  - [x] Added migration and seed baseline contracts plus `check:db`.
  - [x] Added application manifests plus `check:manifests` for app type, object principle, Studio profile, operations, routes, and content capabilities.
  - [x] Added `docs/interface-contracts.md` as the incremental contract/manifest/seed reference.
- evidence:
  - `npm run check:permissions`
  - `npm run check:db`
  - `npm run check:manifests`
  - `npm run test:contracts`
  - `npm run check:quality`
  - `npm run build`
  - `git diff --check` reports line-ending normalization warnings only.
- post-mortem:
  - Operation now has a useful shape without becoming a second permission system.
  - App manifests are intentionally sidecar-only for now; deeper runtime integration should happen when the next app is added.
  - Seed/migration governance is only a contract baseline; an executable runner can be added when schema changes begin.

## 2026-07-16 Codex Increment 7

- domain: design-system, frontend-governance, docs
- ref: working-tree
- branch_context: tapu-master working tree; builds on the existing uncommitted governance sprint.
- todo:
  - [x] Added `tapu/src/styles/design-system.css` as the first shared WhatMint visual token layer.
  - [x] Wired global styles, common utilities, admin aliases, Mint Studio, and NavBar to shared design tokens.
  - [x] Added shared utility classes for surface, focus, muted text, kicker labels, and reduced-motion behavior.
  - [x] Added `docs/design-language.md` to separate design temperament and implementation rules from product manifesto.
- evidence:
  - `npm run build`
  - `npm run check:quality`
  - `git diff --check` reports line-ending normalization warnings only.
- post-mortem:
  - This sprint intentionally avoided repainting every page. The high-value move is a stable visual vocabulary first, then incremental adoption when pages are touched.
  - Existing hardcoded colors remain in older pages; future UI work should migrate them opportunistically to tokens instead of doing a risky broad visual rewrite.

## 2026-07-16 Codex Increment 8

- domain: mint-studio, frontend-ia, content-assets
- ref: working-tree
- branch_context: tapu-master working tree; builds on existing uncommitted governance and Studio work.
- todo:
  - [x] Changed Mint Studio sidebar item click from direct preview open to a content detail panel.
  - [x] Added sidebar search, type filters, and a 24-item visible limit to avoid an endless asset list.
  - [x] Added detail actions for preview, current object connection, connect-to-object for unbound video content, delete video content, and local record removal.
  - [x] Split the detail UI into `MintStudioDetailPanel` and moved detail state/actions into `useMintStudioDetail`.
- evidence:
  - `npm run build`
  - `npm run check:quality`
  - `git diff --check` reports line-ending normalization warnings only.
- post-mortem:
  - Studio now separates creation flow from content-asset inspection. The chat flow stays focused on minting; the detail panel owns lightweight asset actions.
  - Delete remains intentionally narrow: server deletion is only enabled for video content with an existing owner permission route; other asset types show state without pretending to support unsafe deletion.
  - `MintStudioPage.vue` returned from hard-size territory to soft-size territory after extracting panel and detail composable.

## 2026-07-16 Codex Increment 9

- domain: mint-studio, frontend-ia
- ref: working-tree
- todo:
  - [x] Made content detail an exclusive viewing state: expanded detail hides the chat thread and composer.
  - [x] Added a collapsible detail state so users can keep the selected content header while returning to the minting flow.
- evidence:
  - `npm run build`
  - `npm run check:quality`
  - `git diff --check` reports line-ending normalization warnings only.
- post-mortem:
  - Detail view now behaves like inspection, not another message in the creation conversation.

## 2026-07-16 Codex Increment 10

- domain: mint-studio, frontend-ia
- ref: working-tree
- todo:
  - [x] Corrected the collapsible target: content detail is no longer collapsible.
  - [x] Added collapse/expand behavior to the Mint Studio sidebar content list section instead.
  - [x] Kept expanded detail as an exclusive inspection state that hides the chat thread and composer.
- evidence:
  - `npm run build`
  - `npm run check:quality`
  - `git diff --check` reports line-ending normalization warnings only.
- post-mortem:
  - The mental model is now cleaner: sidebar list can collapse for navigation density; detail remains a focused content inspection surface.

## 2026-07-16 Codex Increment 11

- domain: shop, product-experience
- ref: working-tree
- todo:
  - [x] Added a product-detail tap experience entry that opens the official default public content, with first ready public content as fallback.
  - [x] Added a compact touch-experience panel so a product can be felt as a real object entry, not only browsed as a catalog item.
  - [x] Kept the copy user-facing and non-technical, with keyboard access for the panel interaction.
- evidence:
  - `npm run build`
  - `npm run check:quality`
- post-mortem:
  - Product detail now has a clearer bridge from commerce to WhatMint's core behavior: touch the object, enter its small digital space.

## 2026-07-16 Codex Increment 12

- domain: content-operation, whatmint-os
- ref: working-tree
- todo:
  - [x] Added the `meaningful_states` storage layer for OS-level states derived from object events.
  - [x] Added a `contentOperation` service that derives states, builds runtime context, matches app skills, and assembles Daily Sticker crossover content.
  - [x] Extended app manifests with initial `producesStates` and `skills` declarations for emotion IP and Daily Sticker.
  - [x] Recorded video/IP touch resolve events into `object_events`, enabling emotion IP usage to produce meaningful states.
  - [x] Seeded a visible Content Operation demo: earphones slow story unlocks a paper-puppy guest story.
  - [x] Self-tested the demo on a mobile-sized page and added a first-screen relationship notice so the unlocked Mint relationship is immediately visible.
- evidence:
  - `npm run build`
  - `npm run check:quality`
  - Real API check: `/api/daily-stickers/resolve?key=9f1d7a4e6b8c4f21a3d5e7c9b0a2f416` returns `guest_character_story` and the crossover block `纸巾小狗来过`.
- post-mortem:
  - This keeps the first Content Operation layer intentionally small: apps do not read each other's tables; they read OS states and unlocked skills.
  - The demo proves the ecosystem loop without committing to a heavy rules engine yet.

## 2026-07-16 Codex Increment 13

- domain: engineering-governance, os-app-boundary, content-operation
- ref: working-tree
- todo:
  - [x] Split Daily Sticker-specific experience assembly and demo seeding out of `contentOperation` into a Daily Sticker app adapter.
  - [x] Kept `contentOperation` focused on OS responsibilities: meaningful state storage, event-derived states, runtime context, and skill matching.
  - [x] Strengthened app manifest checks for `producesStates` and `skills` shape, key formats, trigger states, and effects.
  - [x] Added Content Operation contract tests for event-to-state derivation, runtime skill unlock, and app adapter content assembly.
  - [x] Added `docs/os-app-boundary.md` to document what belongs to OS and what belongs to app expression.
- evidence:
  - `npm run test:contracts`
  - `npm run check:manifests`
  - `npm run build`
  - `npm run check:quality`
- post-mortem:
  - This round turns the OS/app boundary from a product idea into enforceable engineering shape.
  - The next app should follow the adapter pattern before new app-specific logic leaks into OS services.

## 2026-07-16 Codex Increment 14

- domain: evo-closeout, review, constraints
- ref: working-tree
- todo:
  - [x] Added r6 Codex retro for the OS/App boundary and Content Operation governance sprint.
  - [x] Added r6 S5 observations covering OS maturity, Content Operation MVP validation, and next diagnostic direction.
  - [x] Updated active Codex constraints with OS/App boundary rules for future light-app work.
  - [x] Re-ran build and quality gates before commit.
- evidence:
  - `npm run build`
  - `npm run check:quality`
  - `git diff --check` reports line-ending normalization warnings only.
- post-mortem:
  - The sprint is now closed according to the local evo practice: implementation, validation, review notes, constraints, and ledger are aligned.
  - Current branch is already `tapu-master`; merge is therefore a mainline commit-and-push rather than a feature-branch merge.

## 2026-07-16 Codex Increment 15

- domain: backend, database, whatmint-os, official-management
- ref: working-tree
- branch_context: `evo/core-domain-closeout` cut from `tapu-master`; this round isolates the core-domain cleanup from unrelated local edits.
- todo:
  - [x] Move application registry writes from legacy `applications` into core `application_definitions`.
  - [x] Collapse runtime event writes and app analytics reads onto core `events` instead of legacy `object_events`.
  - [x] Rewire emotion IP adapter lookup to core `ip_instances / ip_definitions / application_definitions`.
  - [x] Stop bootstrap schema from creating legacy `applications` and `object_events` for new databases while keeping backfill compatibility for old databases.
  - [x] Update contract coverage so core event writes and core object resolution are tested directly.
  - [x] Remove obvious process residue: tracked `tapu/public/test.mp4` and stale files under `tapu/server/uploads/temp`.
- evidence:
  - `npm run check:server`
  - `npm run check:permissions`
  - `npm run test:contracts`
  - `npm run check:db`
  - `npm run build`
- post-mortem:
  - The repo still contains legacy compatibility surfaces such as `groups / entities / videos` route naming and backfill readers; this round intentionally removed active duplicate writes first, not every compatibility shell at once.
  - Root note and unrelated working-tree changes remain intentionally out of this commit.

## 2026-07-17 Codex Increment 16

- domain: mint-studio, content-assets, database, evo-closeout
- ref: working-tree
- branch_context: `evo/core-domain-closeout`; closeout stays on the evo branch and does not merge or push.
- todo:
  - [x] Fix Mint Studio content list hot update after login by making Studio library auth state reactive.
  - [x] Dispatch auth change events on a microtask so global listeners and login success handlers observe stable state.
  - [x] Add `content_instance_deletions` as a supporting tombstone table for deleted content assets.
  - [x] Record tombstones in the content delete service before removing `content_instances`.
  - [x] Make Earphone Girl seed and legacy app content sync respect content tombstones.
  - [x] Remove resurrected old synced content rows from the checked-in dev DB and leave tombstones behind.
  - [x] Add S5 r7 observation, retro, and active Codex constraints.
- evidence:
  - `npm run check:quality`
  - `npm run build`
  - sql.js data check: `contentInstances: 7`, `oldPayloadRows: []`, `tombstones: 4`
  - in-memory verification: `syncAnswerBookDeckContent` returns `null` for tombstoned content; Earphone Girl seed does not recreate a tombstoned story.
- post-mortem:
  - The bug was not only a DELETE problem; deterministic seed and sync writers need a durable "do not recreate" fact.
  - Non-reactive auth helpers are fine for one-off requests, but not for computed UI lists.
